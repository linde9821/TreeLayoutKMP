# Superseded by deploy-docs.yml which deploys both the sample and API docs.
# Kept as a placeholder to avoid breaking existing workflow references.
name: Deploy Web Sample (deprecated)

on:
  workflow_dispatch:

jobs:
  redirect:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy-docs.yml instead"
